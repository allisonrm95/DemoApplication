spring.application.name=demo

# Supabase PostgreSQL Configuration
# You can use either format:

# Format 1: JDBC URL (what we had before)
#spring.datasource.url=jdbc:postgresql://db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Format 2: Full connection string from Supabase (just add jdbc: prefix)
spring.datasource.url=jdbc:postgresql://postgres.txppfdftdjgygkoraeqh:doanythingforchewy@aws-0-us-east-1.pooler.supabase.com:5432/postgres

# Note: If using Format 2, you can remove these as they're included in the URL
#spring.datasource.username=postgres
#spring.datasource.password=YOUR_DATABASE_PASSWORD

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Optional: Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
